Feature: Checking and operating the all buttons of calculator application 

Background: 
Given user shoud be in calculator page
Scenario: user go to the application and checking the additon values added or not


When user add the single digit values 
And user add the double digit values 
And user add the triple digit values
Then user add the four digit values

Scenario: user checking the sutraction of values 

When user subtract the single digit value 
And user substract the double digit value
Then user substract the four digit value

Scenario: user checking the multiplying of values

When user multiply the single digit value
And user multiply the double digit value
Then user mutiply the double digit with point values

Scenario: user checking the division of values

When user divide the values of single digit 
And user divide the values of double digit 
And user divide the values of triple digit 
Then user divide the values of four digit

Scenario: user checking to click the buttons of the application

When user click to check the dot button
Then user click to check the clear enter button CE


